services:
  - type: web
    name: sinceai-api
    runtime: docker
    dockerfilePath: ./python-stuff/dockerfile
    dockerContext: ./python-stuff
    region: frankfurt  # EU region, change if needed
    plan: free
    healthCheckPath: /health
    envVars:
      - key: PYTHONUNBUFFERED
        value: "1"
